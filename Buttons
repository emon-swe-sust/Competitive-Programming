#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,i,k,ans=0;
    cin >> n;
    for(i=1;i<=n;i++)
    {
        ans = ans + (n-i)*(i)+1;
    }
    cout << ans << endl;
    return 0;
}
