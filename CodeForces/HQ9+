#include<stdio.h>
#include<string.h>

int main()
{
    char p[100];
    scanf("%s",p);

    int i,j,flag=1;

    i=strlen(p);

    for(j=0;j<i;j++)
    {
        if(p[j]=='H'||p[j]=='Q'||p[j]=='9')
        {
            flag=2;
            break;
        }

    }

    if(flag==1)
        printf("NO");
    else
        printf("YES");

    return 0;
}
