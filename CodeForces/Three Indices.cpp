#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while(t--)
    {
        int n,i,j,min=0,max=0,sm=0,bg=0;
        cin >> n;
        int arr[n];
        for(i=0;i<n;i++)
        {
            cin >> arr[i];
        }
    }
}