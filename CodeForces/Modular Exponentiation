#include<bits/stdc++.h>
using namespace std;

int main()
{
    long long m,n,k=1,i;
    cin >> n >> m;
    if( n < 30)
    {
        for(i=0; i<n; i++)
        {
            k=2*k;
        }
        cout << m%k << endl;
    }
    else
    {
        cout << m << endl;
    }
    return 0;
}
