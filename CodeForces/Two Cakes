#include<bits/stdc++.h>
using namespace std;

int main()
{
   int a,b,n,i,j,ans;
   cin >> n >> a >> b;
   if(a>b)
        swap(a,b);
       // cout << a << b;
   for(i=1;i<=a;i++)
   {
       if(a/i+b/i >= n)
        ans = i;
   }
    cout << ans << endl;
    return 0;
}
