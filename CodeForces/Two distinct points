#include<bits/stdc++.h>
using namespace std;

int main()
{
    int q;
    cin >> q;
    while(q--)
    {
        int a1,a2,b1,b2,a,b;
        cin >> a1 >> a2 >>b1 >> b2 ;
        if(a1!=b2)
        {
            cout << a1 << " " << b2 << endl;
            continue;
        } 
        else
        {
            cout << a1 << " " << b2-1 << endl;
        }
    }
    return 0;
}
