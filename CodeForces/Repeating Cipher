#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,i,st=1;
    cin >> n;
    string str;
    cin >> str;
    for(i=0;i<n;i++)
    {
        st += i;
        if(st-1 < n)
            cout << str[st-1];
    }
    cout << endl;
    return 0;
}
