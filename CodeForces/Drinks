#include<stdio.h>

int main()
{
    int n,i;
    scanf("%d",&n);

    double a[n];

    double result,sum=0;

    for(i=0;i<n;i++)
    {
        scanf("%lf",&a[i]);
    }

    for(i=0;i<n;i++)
    {
        sum=sum+a[i];
    }

    result=sum/n;

    printf("%lf",result);

    return 0;
}
